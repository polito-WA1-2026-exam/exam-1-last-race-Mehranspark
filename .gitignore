# Local Claude Code context — not part of the submission
CLAUDE.md
.claude/

# Local notes & understanding docs — not part of the submission
local-notes/

# Node
node_modules/
npm-debug.log*

# Env / local
.env
.env.local
.DS_Store

# Build output
dist/
build/

# SQLite transient sidecar files (the main .sqlite IS committed, these are not)
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
